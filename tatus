warning: in the working copy of 'Brass_QC/services/validators.py', LF will be replaced by CRLF the next time Git touches it
[1mdiff --git a/Brass_QC/services/validators.py b/Brass_QC/services/validators.py[m
[1mindex f95eebf..9f08fdd 100644[m
[1m--- a/Brass_QC/services/validators.py[m
[1m+++ b/Brass_QC/services/validators.py[m
[36m@@ -1,5 +1,5 @@[m
 """[m
[31m-Brass QC Validators — input validation only.[m
[32m+[m[32mBrass QC Validators â€” input validation only.[m
 [m
 All submission and tray scan validation lives here.[m
 Returns (is_valid: bool, error_str: str | None).[m
[36m@@ -190,6 +190,40 @@[m [mdef _snapshot_has_tray(snapshot_data, tid):[m
     return False[m
 [m
 [m
[32m+[m[32mdef _is_brass_qc_reject_tray_explicitly_released(tray_id):[m
[32m+[m[32m    """True only when a BQC reject-history tray was explicitly released."""[m
[32m+[m[32m    tid = _norm_tray_id(tray_id)[m
[32m+[m[32m    if not tid:[m
[32m+[m[32m        return False[m
[32m+[m
[32m+[m[32m    tray = TrayId.objects.filter(tray_id__iexact=tid).first()[m
[32m+[m[32m    return bool(tray and tray.delink_tray and not tray.scanned)[m
[32m+[m
[32m+[m
[32m+[m[32mdef _has_brass_qc_tray_level_rejection_evidence(tray_id):[m
[32m+[m[32m    """Return True for tray-level BQC reject rows that still look active."""[m
[32m+[m[32m    from ..models import Brass_QC_Rejected_TrayScan, BrassTrayId[m
[32m+[m
[32m+[m[32m    tid = _norm_tray_id(tray_id)[m
[32m+[m[32m    if not tid:[m
[32m+[m[32m        return False[m
[32m+[m
[32m+[m[32m    if Brass_QC_Rejected_TrayScan.objects.filter([m
[32m+[m[32m        rejected_tray_id__iexact=tid,[m
[32m+[m[32m    ).exclude([m
[32m+[m[32m        rejected_tray_id__isnull=True,[m
[32m+[m[32m    ).exclude([m
[32m+[m[32m        rejected_tray_id="",[m
[32m+[m[32m    ).exists():[m
[32m+[m[32m        return True[m
[32m+[m
[32m+[m[32m    return BrassTrayId.objects.filter([m
[32m+[m[32m        tray_id__iexact=tid,[m
[32m+[m[32m        rejected_tray=True,[m
[32m+[m[32m        delink_tray=False,[m
[32m+[m[32m    ).exists()[m
[32m+[m
[32m+[m
 def is_tray_rejected_in_brass_qc(tray_id):[m
     """[m
     Return True when this tray was rejected during Brass QC processing and has[m
[36m@@ -198,7 +232,7 @@[m [mdef is_tray_rejected_in_brass_qc(tray_id):[m
     Brass QC does not flag rejected trays on a live tray table (rejected trays[m
     are recorded only inside Brass_QC_Submission's reject snapshots and the[m
     BrassQC_PartialRejectLot snapshot), so those snapshots are the source of[m
[31m-    truth here — a downstream module like Brass Audit must check them before[m
[32m+[m[32m    truth here â€” a downstream module like Brass Audit must check them before[m
     letting a scanned tray_id be accepted.[m
     """[m
     from ..models import Brass_QC_Submission, BrassQC_PartialRejectLot[m
[36m@@ -206,7 +240,7 @@[m [mdef is_tray_rejected_in_brass_qc(tray_id):[m
     tid = _norm_tray_id(tray_id)[m
     if not tid:[m
         return False[m
[31m-    if is_tray_released_for_reuse(tid):[m
[32m+[m[32m    if _is_brass_qc_reject_tray_explicitly_released(tid):[m
         return False[m
 [m
     for submission in Brass_QC_Submission.objects.exclude([m
[36m@@ -224,12 +258,15 @@[m [mdef is_tray_rejected_in_brass_qc(tray_id):[m
             if _norm_tray_id(tray.get("tray_id")) == tid and _safe_int(tray.get("qty")) > 0:[m
                 return True[m
 [m
[32m+[m[32m    if _has_brass_qc_tray_level_rejection_evidence(tid):[m
[32m+[m[32m        return True[m
[32m+[m
     return False[m
 [m
 [m
[31m-# ─────────────────────────────────────────────────────────────────────────────[m
[32m+[m[32m# â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€[m
 # Submission validators[m
[31m-# ─────────────────────────────────────────────────────────────────────────────[m
[32m+[m[32m# â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€[m
 [m
 def validate_not_duplicate_submit(lot_id, is_iqf_reentry=False):[m
     """[m
[36m@@ -256,11 +293,11 @@[m [mdef validate_full_reject_reasons(rejection_reasons, total_qty):[m
     Returns error string or None.[m
     """[m
     if not rejection_reasons:[m
[31m-        return None  # ✅ Allow empty — backend will auto-fill[m
[32m+[m[32m        return None  # âœ… Allow empty â€” backend will auto-fill[m
     [m
     total = sum(int(r.get("qty", 0)) for r in rejection_reasons)[m
     [m
[31m-    # ✅ FIX: Allow reasons to sum to <= total_qty (not must equal)[m
[32m+[m[32m    # âœ… FIX: Allow reasons to sum to <= total_qty (not must equal)[m
     # Missing qty will be auto-filled by backend[m
     if total > total_qty:[m
         return ([m
[36m@@ -293,7 +330,7 @@[m [mdef validate_process_tray_actions(tray_actions, active_trays, stock, lot_id):[m
     Also handles:[m
     - New reject trays not in this lot (validates against TrayId master)[m
     - IS-rejected tray blocking[m
[31m-    - Delink actions (writes BrassTrayId, TrayId delink flags — side effect only here)[m
[32m+[m[32m    - Delink actions (writes BrassTrayId, TrayId delink flags â€” side effect only here)[m
     """[m
     from ..models import BrassTrayId[m
     if not tray_actions:[m
[36m@@ -324,16 +361,17 @@[m [mdef validate_process_tray_actions(tray_actions, active_trays, stock, lot_id):[m
             if ta_action == "ACCEPT":[m
                 return [], [], validate_accept_tray_current_lot(tid, active_trays)[m
             if ta_action == "REJECT":[m
[31m-                # New tray (not in this lot) scanned into a reject slot — validate master[m
[32m+[m[32m                # New tray (not in this lot) scanned into a reject slot â€” validate master[m
                 if not TrayId.objects.filter(tray_id=tid).exists():[m
                     return [], [], f"Reject tray '{tid}' not found in master tray list"[m
 [m
[31m-                # Block only true IS rejects. Released/delink-only trays remain reusable.[m
[31m-                if is_tray_rejected_in_input_screening(tid):[m
[31m-                    return [], [], ([m
[31m-                        f"Tray '{tid}' was rejected in Input Screening — "[m
[31m-                        f"permanently ineligible for reuse"[m
[31m-                    )[m
[32m+[m[32m                # Brass QC-specific cross-module occupancy guard.  Keep the[m
[32m+[m[32m                # existing shared validator unchanged because it is also consumed[m
[32m+[m[32m                # by other modules; this wrapper adds Nickel Wiping/Audit coverage[m
[32m+[m[32m                # only for Brass QC scan/submission paths.[m
[32m+[m[32m                occupied_module, _occupied_error = validate_brass_qc_tray_occupancy(tid, lot_id)[m
[32m+[m[32m                if occupied_module:[m
[32m+[m[32m                    return [], [], "Tray is occupied."[m
 [m
                 slot_qty = int(ta.get("qty") or 0)[m
                 if slot_qty <= 0:[m
[36m@@ -355,7 +393,7 @@[m [mdef validate_process_tray_actions(tray_actions, active_trays, stock, lot_id):[m
             rejected_trays.append({"tray_id": tid, "qty": slot_qty, "is_top": is_top})[m
 [m
         elif ta_action == "DELINK":[m
[31m-            # Write delink flags — this is the only write in validators (necessary side effect)[m
[32m+[m[32m            # Write delink flags â€” this is the only write in validators (necessary side effect)[m
             BrassTrayId.objects.filter(lot_id=lot_id, tray_id=tid).update(delink_tray=True)[m
             TrayId.objects.filter(lot_id=lot_id, tray_id=tid).update(delink_tray=True)[m
 [m
[36m@@ -370,9 +408,9 @@[m [mdef validate_process_tray_actions(tray_actions, active_trays, stock, lot_id):[m
     return accepted_trays, rejected_trays, None[m
 [m
 [m
[31m-# ─────────────────────────────────────────────────────────────────────────────[m
[32m+[m[32m# â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€[m
 # Tray scan validators[m
[31m-# ─────────────────────────────────────────────────────────────────────────────[m
[32m+[m[32m# â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€[m
 [m
 def validate_tray_not_rejected_in_is(tray_id):[m
     """[m
[36m@@ -396,7 +434,7 @@[m [mdef validate_tray_not_rejected_in_brass_qc(tray_id):[m
     Returns None if tray is eligible.[m
 [m
     Covers Brass_QC_Submission reject snapshots and BrassQC_PartialRejectLot[m
[31m-    snapshots — the only places Brass QC records a rejected tray_id.[m
[32m+[m[32m    snapshots â€” the only places Brass QC records a rejected tray_id.[m
     """[m
     if is_tray_rejected_in_brass_qc(tray_id):[m
         return ([m
[36m@@ -605,3 +643,115 @@[m [mdef validate_tray_cross_module_occupancy(tray_id, lot_id):[m
         return "Assigned Lot", "Tray is currently occupied in an assigned lot"[m
 [m
     return None, None[m
[32m+[m[32mdef validate_brass_qc_tray_occupancy(tray_id, lot_id=None):[m
[32m+[m[32m    """[m
[32m+[m[32m    Brass QC-only occupancy wrapper used by tray scan and PROCESS submit.[m
[32m+[m
[32m+[m[32m    It deliberately leaves ``validate_tray_cross_module_occupancy`` unchanged[m
[32m+[m[32m    because that shared helper is consumed by other modules.  This wrapper adds[m
[32m+[m[32m    the missing Nickel Wiping / Nickel Audit checks while preserving each[m
[32m+[m[32m    module's release-aware lifecycle and the current-lot exception.[m
[32m+[m
[32m+[m[32m    Returns (module_name, error_str) when blocked, otherwise (None, None).[m
[32m+[m[32m    """[m
[32m+[m[32m    tid = _norm_tray_id(tray_id)[m
[32m+[m[32m    current_lot = str(lot_id or "").strip()[m
[32m+[m[32m    if not tid:[m
[32m+[m[32m        return None, None[m
[32m+[m
[32m+[m[32m    # Nickel Wiping Z1/Z2 share one backend lifecycle helper.  It only treats[m
[32m+[m[32m    # historical reject snapshots as blocking while their owner lot is active,[m
[32m+[m[32m    # so legitimately released/delinked trays remain reusable.[m
[32m+[m[32m    try:[m
[32m+[m[32m        from Nickel_Inspection.services import validate_nickel_wiping_rejection_tray_available[m
[32m+[m
[32m+[m[32m        nw_available, _nw_message = validate_nickel_wiping_rejection_tray_available([m
[32m+[m[32m            tid,[m
[32m+[m[32m            current_lot_id=current_lot or None,[m
[32m+[m[32m        )[m
[32m+[m[32m        if not nw_available:[m
[32m+[m[32m            return "Nickel Wiping", "Tray is occupied."[m
[32m+[m[32m    except ImportError:[m
[32m+[m[32m        logger.exception([m
[32m+[m[32m            "[Brass QC] Nickel Wiping tray validator import failed for tray_id=%s",[m
[32m+[m[32m            tid,[m
[32m+[m[32m        )[m
[32m+[m
[32m+[m[32m    # Nickel Audit Z1/Z2 share the same backend models.  Do NOT rely only on[m
[32m+[m[32m    # Nickel_AuditTrayId here: after a partial rejection the live mirror/master[m
[32m+[m[32m    # row can be cleared while the physical reject tray is still represented by[m
[32m+[m[32m    # Nickel Audit reject scans/submission snapshots.  A reject snapshot remains[m
[32m+[m[32m    # blocking until the tray is explicitly delinked/released in the master.[m
[32m+[m[32m    try:[m
[32m+[m[32m        from Nickel_Audit.models import ([m
[32m+[m[32m            Nickel_AuditTrayId,[m
[32m+[m[32m            Nickel_Audit_Rejected_TrayScan,[m
[32m+[m[32m            NickelAudit_Submission,[m
[32m+[m[32m            NickelAudit_PartialRejectLot,[m
[32m+[m[32m        )[m
[32m+[m
[32m+[m[32m        master_tray = TrayId.objects.filter(tray_id__iexact=tid).first()[m
[32m+[m[32m        explicitly_released = bool([m
[32m+[m[32m            master_tray[m
[32m+[m[32m            and master_tray.delink_tray[m
[32m+[m[32m            and not master_tray.scanned[m
[32m+[m[32m        )[m
[32m+[m
[32m+[m[32m        if not explicitly_released:[m
[32m+[m[32m            # Live Nickel Audit ownership (both zones use the same model).[m
[32m+[m[32m            na_qs = Nickel_AuditTrayId.objects.filter([m
[32m+[m[32m                tray_id__iexact=tid,[m
[32m+[m[32m                delink_tray=False,[m
[32m+[m[32m                lot_id__isnull=False,[m
[32m+[m[32m            )[m
[32m+[m[32m            if na_qs.exists():[m
[32m+[m[32m                return "Nickel Audit", "Tray is occupied."[m
[32m+[m
[32m+[m[32m            # Reject scan rows are the direct physical reject-tray record.[m
[32m+[m[32m            if Nickel_Audit_Rejected_TrayScan.objects.filter([m
[32m+[m[32m                rejected_tray_id__iexact=tid,[m
[32m+[m[32m            ).exists():[m
[32m+[m[32m                return "Nickel Audit", "Tray is occupied."[m
[32m+[m
[32m+[m[32m            # Current submission snapshots are also checked because some Nickel[m
[32m+[m[32m            # Audit paths persist reject tray identity there even when the mirror[m
[32m+[m[32m            # row has already been cleaned up.[m
[32m+[m[32m            for submission in NickelAudit_Submission.objects.filter([m
[32m+[m[32m                submission_type__in=["PARTIAL", "FULL_REJECT"],[m
[32m+[m[32m            ).only("reject_trays_data"):[m
[32m+[m[32m                for row in submission.reject_trays_data or []:[m
[32m+[m[32m                    if not isinstance(row, dict):[m
[32m+[m[32m                        continue[m
[32m+[m[32m                    if ([m
[32m+[m[32m                        _norm_tray_id(row.get("tray_id") or row.get("rejected_tray_id")) == tid[m
[32m+[m[32m                        and _safe_int(row.get("qty") or row.get("tray_quantity")) > 0[m
[32m+[m[32m                    ):[m
[32m+[m[32m                        return "Nickel Audit", "Tray is occupied."[m
[32m+[m
[32m+[m[32m            # Partial-reject child snapshots provide the same protection for[m
[32m+[m[32m            # split-lot records.[m
[32m+[m[32m            for reject_lot in NickelAudit_PartialRejectLot.objects.exclude([m
[32m+[m[32m                trays_snapshot__isnull=True,[m
[32m+[m[32m            ).only("trays_snapshot"):[m
[32m+[m[32m                for row in reject_lot.trays_snapshot or []:[m
[32m+[m[32m                    if not isinstance(row, dict):[m
[32m+[m[32m                        continue[m
[32m+[m[32m                    if ([m
[32m+[m[32m                        _norm_tray_id(row.get("tray_id") or row.get("rejected_tray_id")) == tid[m
[32m+[m[32m                        and _safe_int(row.get("qty") or row.get("tray_quantity")) > 0[m
[32m+[m[32m                    ):[m
[32m+[m[32m                        return "Nickel Audit", "Tray is occupied."[m
[32m+[m[32m    except ImportError:[m
[32m+[m[32m        logger.exception([m
[32m+[m[32m            "[Brass QC] Nickel Audit tray model import failed for tray_id=%s",[m
[32m+[m[32m            tid,[m
[32m+[m[32m        )[m
[32m+[m
[32m+[m[32m    # Existing shared validation remains the authority for Input Screening,[m
[32m+[m[32m    # Brass QC, Brass Audit, IQF, Brass Audit drafts, Jig Loading excess and[m
[32m+[m[32m    # master ownership.  We intentionally do not alter that helper's behavior.[m
[32m+[m[32m    module_name, error = validate_tray_cross_module_occupancy(tid, current_lot)[m
[32m+[m[32m    if module_name:[m
[32m+[m[32m        return module_name, error or "Tray is occupied."[m
[32m+[m
[32m+[m[32m    return None, None[m
